#!/usr/bin/env node
/**
 * Enrich DSA5101 Lecture 1 slides with slide-specific study layers.
 * Replaces the generic/duplicated explanation and socraticQuestions
 * with content-specific whatYouSee/whyItMatters/intuition/technicalDetail/
 * pitfall/connection and Socratic checkpoints, matching the DSA5105 pattern.
 *
 * Run: node scripts/enrich-dsa5101-slides.js
 */
const fs = require('fs');
const path = require('path');

const FILE = path.join(__dirname, '..', 'content', 'courses', 'DSA5101', 'slides', 'dsa5101-lecture1.json');

// Compact per-slide study metadata: slideNumber -> { concept, why, pitfall, qType, qPrompt, qAnswer, qHint }
const META = {
  1: { concept: "course identity and instructor", why: "establishes the course scope and who is teaching before any technical content", pitfall: "treating the title slide as evidence about technical content", qType: "orient", qPrompt: "What does the course title tell you about the intended audience and focus?", qAnswer: "It targets industry applications of big data, so the emphasis is on scalable, practical techniques rather than purely theoretical results.", qHint: "Read the title as a scope statement." },
  2: { concept: "assessment weights and logistics", why: "defines how the course is assessed and where materials live, shaping study effort allocation", pitfall: "confusing an assessment weight with a learning objective or a confirmed date", qType: "recall", qPrompt: "Which assessment carries the largest weight and what does that imply for study strategy?", qAnswer: "The final exam at 50% dominates, so prioritize conceptual understanding and derivations over memorizing implementation details.", qHint: "Weight reflects emphasis." },
  3: { concept: "lecture logistics and reference textbook", why: "identifies the official textbook and communication channels as primary study resources", pitfall: "treating the textbook as the sole source of truth; the lecture defines the examinable scope", qType: "orient", qPrompt: "Why is it useful to know the reference textbook before starting the technical content?", qAnswer: "It tells you where to find deeper explanations and worked examples when the lecture slides are too terse.", qHint: "Textbook = depth layer." },
  4: { concept: "section divider into the course content", why: "marks a change in abstraction level from logistics to the core question of what data mining is", pitfall: "skipping the divider mentally when studying linearly", qType: "explain", qPrompt: "Why must a data-mining course begin by motivating the value of data before teaching algorithms?", qAnswer: "Because the algorithms only make sense once you understand what patterns are worth finding and why scale changes the problem.", qHint: "Motivation precedes method." },
  5: { concept: "visual motivation for data mining", why: "reinforces the practical, industry-facing motivation before technical definitions begin", pitfall: "inferring specific technical claims from a slide whose text extraction is incomplete", qType: "critique", qPrompt: "What kind of value can be extracted from data, and what could go wrong if the data is too large to process naively?", qAnswer: "Patterns and decisions can be extracted, but scale makes naive processing infeasible, which is the course's core concern.", qHint: "Scale is the course's core concern." },
  6: { concept: "data contains value and knowledge", why: "establishes the premise that raw data holds extractable value, motivating the whole course", pitfall: "assuming value is automatically accessible without the right algorithms and scale-aware methods", qType: "explain", qPrompt: "What does it mean for data to 'contain value and knowledge'?", qAnswer: "It means patterns and insights are latent in the data but require appropriate mining techniques to extract, especially at scale.", qHint: "Value is latent, not automatic." },
  7: { concept: "data mining definition and pipeline", why: "defines data mining as extraction of information from very large datasets and positions it within storage, management, and analysis", pitfall: "equating data mining with machine learning or predictive analytics without noting the scale emphasis", qType: "define", qPrompt: "How does the slide define data mining and where does it sit in the data pipeline?", qAnswer: "Data mining is extraction of information from very large datasets, sitting after storage and management as the analysis step.", qHint: "Focus on the 'very large datasets' qualifier." },
  8: { concept: "descriptive vs predictive methods", why: "introduces the two broad families of data-mining methods: describing data vs predicting future values", pitfall: "treating the two families as mutually exclusive when many real systems combine both", qType: "compare", qPrompt: "What is the difference between descriptive and predictive data-mining methods?", qAnswer: "Descriptive methods describe data in a human-interpretable way (e.g., clustering); predictive methods use variables to predict unknown future values (e.g., recommender systems).", qHint: "Describe vs predict." },
  9: { concept: "demand for data mining", why: "motivates the course by showing industry demand for data-mining skills", pitfall: "treating demand as evidence about specific algorithms", qType: "orient", qPrompt: "Why does industry demand for data mining matter for how you should study this course?", qAnswer: "It signals that the practical, scalable techniques taught here are directly applicable in industry roles.", qHint: "Demand = relevance." },
  10: { concept: "Bonferroni's Principle", why: "warns that if you look hard enough for a pattern, you are bound to find one, even by chance", pitfall: "ignoring the multiple-comparisons problem when evaluating discovered patterns", qType: "explain", qPrompt: "What is Bonferroni's Principle and why does it matter in data mining?", qAnswer: "It states that if you search many combinations, you will find spurious patterns by chance; you must account for the search space when judging significance.", qHint: "Searching many combinations creates false discoveries." },
  11: { concept: "hunting terrorists example", why: "gives a concrete scenario where Bonferroni's Principle produces many false positives", pitfall: "treating the example as a real surveillance recommendation rather than a cautionary calculation", qType: "apply", qPrompt: "Why does the terrorist-hunting example produce so many suspicious pairs?", qAnswer: "Because the number of person-pairs and day-pairs is enormous, so even tiny per-pair probabilities yield many expected coincidences.", qHint: "Multiply large counts by small probabilities." },
  12: { concept: "approximate calculation of false positives", why: "shows the arithmetic behind Bonferroni's Principle: 250,000 expected suspicious pairs from random coincidence", pitfall: "forgetting that the calculation is an expectation, not a guarantee", qType: "derive", qPrompt: "Walk through the calculation that yields 250,000 expected suspicious pairs.", qAnswer: "5e17 person-pairs x 5e5 day-pairs x 1e-18 per-pair probability = 250,000 expected coincidences.", qHint: "Multiply the three factors." },
  13: { concept: "course scope: what this class is and is not", why: "sets expectations: a wide range of scalable data-mining techniques, not a comprehensive survey or a pure math class", pitfall: "expecting a proof-heavy or API-focused course when the emphasis is on scalable algorithms", qType: "scope", qPrompt: "What does the course promise and what does it explicitly exclude?", qAnswer: "It promises a wide range of data-mining techniques focused on scalable algorithms, and excludes a comprehensive survey and a pure math class.", qHint: "Read the checkmarks and crosses." },
  14: { concept: "data types and computation models", why: "maps the course scope: high-dimensional data, streams, graphs, labeled/unlabeled, and single-machine, online, and MapReduce models", pitfall: "treating the computation models as interchangeable without noting their different constraints", qType: "compare", qPrompt: "What data types and computation models does the course cover?", qAnswer: "High-dimensional, infinite streams, graphs, and labeled/unlabeled data, computed via single-machine in-memory, online, and MapReduce models.", qHint: "Data types vs computation models." },
  15: { concept: "real-world problems and tools", why: "connects course techniques to market basket analysis, recommender systems, spam detection, and duplicate detection, using DP, linear algebra, optimization, and hashing", pitfall: "memorizing the tool list without understanding which tool fits which problem", qType: "apply", qPrompt: "Which tool would you reach for in duplicate-document detection and why?", qAnswer: "Hashing (e.g., MinHash/LSH) because it efficiently estimates similarity between large sets without pairwise comparison.", qHint: "Similarity at scale." },
  16: { concept: "course map: data types to applications", why: "shows the full landscape connecting high-dimensional, graph, and stream data to applications like LSH, PageRank, recommender systems, and clustering", pitfall: "treating the map as exhaustive when modern systems combine multiple techniques", qType: "orient", qPrompt: "How does the course map connect data types to applications?", qAnswer: "It shows which techniques (LSH, PageRank, clustering, etc.) apply to which data types (high-dim, graph, stream) and applications.", qHint: "Data type drives technique choice." },
  17: { concept: "section divider into frequent itemsets and association rules", why: "marks the transition from course overview to the first concrete topic", pitfall: "skipping the divider and losing the narrative thread", qType: "orient", qPrompt: "What topic does this section introduce and why is it foundational?", qAnswer: "Frequent itemsets and association rules, which are foundational for market basket analysis and many other pattern-mining tasks.", qHint: "Patterns in baskets." },
  18: { concept: "association rule discovery and market-basket model", why: "introduces the goal of identifying items bought together and the classic diapers-beer example", pitfall: "treating the diapers-beer rule as a guaranteed finding rather than an illustrative example", qType: "explain", qPrompt: "What is the goal of association rule discovery in the market-basket model?", qAnswer: "To identify items that are bought together by many customers, revealing dependencies among items.", qHint: "Co-occurrence patterns." },
  19: { concept: "market-basket model input/output", why: "defines items, baskets, and the desired output of association rules with a concrete example", pitfall: "confusing the input (baskets of items) with the output (rules)", qType: "define", qPrompt: "What are the inputs and outputs of the market-basket model?", qAnswer: "Inputs are a large set of items and a large set of baskets (each a small subset of items); output is association rules like {Diaper, Milk} -> {Beer}.", qHint: "Input = baskets; output = rules." },
  20: { concept: "generalization of items/baskets", why: "shows the model generalizes beyond products to amino acids/proteins and words/documents", pitfall: "limiting the model to retail when it applies to any many-to-many association", qType: "apply", qPrompt: "How does the items/baskets model generalize beyond supermarket data?", qAnswer: "Items and baskets can be any many-to-many association, e.g., amino acids/proteins or words/documents.", qHint: "Many-to-many mapping." },
  21: { concept: "application: market baskets", why: "shows how real stores use basket data to position items and run tie-in promotions", pitfall: "treating the application as the only use case", qType: "apply", qPrompt: "How do stores use market-basket data in practice?", qAnswer: "To understand how customers navigate stores, position tempting items, and run tie-in promotions like sales on diapers with higher beer prices.", qHint: "Store layout and pricing." },
  22: { concept: "application: plagiarism detection", why: "shows items can be documents and baskets can be sentences, with co-occurring items indicating plagiarism", pitfall: "treating co-occurrence as proof of plagiarism without further evidence", qType: "apply", qPrompt: "How is the items/baskets model applied to plagiarism detection?", qAnswer: "Documents are items and sentences are baskets; items appearing together too often could represent plagiarism.", qHint: "Co-occurrence as a signal." },
  23: { concept: "lecture outline", why: "maps the lecture: define frequent itemsets and association rules, then algorithms for finding them", pitfall: "skipping the outline and losing the structure", qType: "orient", qPrompt: "What are the two main parts of this lecture?", qAnswer: "Defining frequent itemsets and association rules (support, confidence, interestingness), then algorithms for finding frequent itemsets.", qHint: "Definition then algorithm." },
  24: { concept: "frequent itemsets and support", why: "defines support as the number of baskets containing all items in a set, and frequent itemsets as those meeting a threshold", pitfall: "confusing support (count) with confidence (conditional probability)", qType: "define", qPrompt: "What is the support of an itemset and what makes it frequent?", qAnswer: "Support is the number of baskets containing all items in the set; an itemset is frequent if its support meets a threshold s.", qHint: "Support = count." },
  25: { concept: "worked example of frequent itemsets", why: "shows how to compute frequent itemsets from a concrete basket dataset with a support threshold", pitfall: "forgetting to check all subsets, not just singletons", qType: "apply", qPrompt: "In the example, why is {m,b} frequent but {m,p} not?", qAnswer: "{m,b} appears in at least 3 baskets while {m,p} appears in fewer than the threshold of 3.", qHint: "Count each candidate." },
  26: { concept: "association rules and confidence", why: "defines association rules as if-then statements and confidence as the conditional probability of the consequent given the antecedent", pitfall: "confusing confidence with support", qType: "define", qPrompt: "What is the confidence of an association rule I -> j?", qAnswer: "It is the probability of j given I, computed as support(I union {j}) / support(I).", qHint: "Conditional probability." },
  27: { concept: "interestingness of association rules", why: "introduces interest as the difference between confidence and the base rate of the consequent, to filter trivial rules", pitfall: "treating high confidence as sufficient for interestingness", qType: "explain", qPrompt: "Why is a high-confidence rule not necessarily interesting?", qAnswer: "Because if the consequent is already very common, the rule adds little information; interest measures the lift over the base rate.", qHint: "Compare to base rate." },
  28: { concept: "worked example of confidence and interest", why: "shows the calculation of confidence and interest for a specific rule, demonstrating low interestingness", pitfall: "forgetting to subtract the base rate when computing interest", qType: "apply", qPrompt: "In the example, why is the rule {m,b} -> c not very interesting?", qAnswer: "Its confidence is 0.5 but c appears in 5/8 of baskets, so interest is only |0.5 - 5/8| = 1/8, which is low.", qHint: "Subtract the base rate." },
  29: { concept: "finding association rules with support and confidence thresholds", why: "defines the problem as finding all rules with support >= s and confidence >= c, noting the hard part is finding frequent itemsets", pitfall: "forgetting that rule support is the support of the union of left and right sides", qType: "define", qPrompt: "What is the support of an association rule and why is finding frequent itemsets the hard part?", qAnswer: "Rule support is the support of the union of left and right sides; finding frequent itemsets is hard because the search space is exponential.", qHint: "Union support." },
  30: { concept: "mining association rules in two steps", why: "breaks the problem into finding frequent itemsets then generating rules, with monotonicity pruning", pitfall: "forgetting the monotonicity property that prunes rule generation", qType: "explain", qPrompt: "What are the two steps of mining association rules and how does monotonicity help?", qAnswer: "Find all frequent itemsets, then generate rules; monotonicity says if a rule is below confidence, adding items to the left cannot raise it.", qHint: "Monotonicity prunes." },
  31: { concept: "worked example of rule generation", why: "shows the full pipeline from frequent itemsets to rule generation with confidence thresholds", pitfall: "miscounting support when computing confidence", qType: "apply", qPrompt: "In the example, how is the confidence of b -> m computed?", qAnswer: "It is support({b,m}) / support({b}) = 4/6, since b appears in 6 baskets and {b,m} in 4.", qHint: "Divide by antecedent support." },
  32: { concept: "section divider into finding frequent itemsets", why: "marks the transition from definitions to algorithms", pitfall: "skipping the divider and losing the algorithmic focus", qType: "orient", qPrompt: "What does this section focus on?", qAnswer: "Algorithms for finding frequent itemsets efficiently, given the scale of real data.", qHint: "Algorithms at scale." },
  33: { concept: "computation model for itemsets", why: "describes how data is stored on disk basket-by-basket and that the major cost is reading baskets from disk", pitfall: "ignoring the disk-I/O cost when analyzing algorithm complexity", qType: "explain", qPrompt: "What is the major cost of a frequent-itemset algorithm and why?", qAnswer: "The time to read baskets from disk to memory, since data is too big to fit in main memory and is stored basket-by-basket.", qHint: "Disk I/O dominates." },
  34: { concept: "communication cost as the key metric", why: "establishes that running time is proportional to the number of passes through the data times data size", pitfall: "measuring algorithm speed by raw runtime instead of number of passes", qType: "define", qPrompt: "Why is the number of passes the key measure of algorithm speed?", qAnswer: "Because data size is fixed and uncontrollable, so the number of passes through the data is the controllable cost.", qHint: "Passes = controllable cost." },
  35: { concept: "main-memory bottleneck", why: "identifies main memory as the limiting factor for many frequent-itemset algorithms", pitfall: "ignoring memory constraints when designing algorithms", qType: "explain", qPrompt: "Why is main memory the limiting factor for frequent-itemset algorithms?", qAnswer: "Because as we read baskets we must count pairs or other itemsets, and the number of things we can count is limited by main memory.", qHint: "Counting needs memory." },
  36: { concept: "finding frequent pairs as the hardest problem", why: "notes that frequent pairs are common while frequent triples are rare, so pairs are the focus first", pitfall: "assuming larger itemsets are as easy to find as pairs", qType: "explain", qPrompt: "Why is finding frequent pairs often the hardest problem?", qAnswer: "Because frequent pairs are common and numerous, while the probability of being frequent drops exponentially with itemset size.", qHint: "Pairs are the bottleneck." },
  37: { concept: "counting pairs in memory: matrix vs triples", why: "compares two approaches: a triangular matrix (4 bytes per pair) vs a table of triples (12 bytes per occurring pair)", pitfall: "forgetting the memory trade-off depends on how many pairs actually occur", qType: "compare", qPrompt: "What is the memory trade-off between the triangular matrix and triples approaches?", qAnswer: "The matrix uses 4 bytes per pair but always allocates for all pairs; triples use 12 bytes per occurring pair but only for pairs with count > 0.", qHint: "Fixed vs sparse." },
  38: { concept: "visual comparison of matrix vs triples", why: "shows the memory layout of both approaches side by side", pitfall: "treating the visual as the full analysis rather than a summary", qType: "orient", qPrompt: "What does the visual comparison show about the two approaches?", qAnswer: "It shows the triangular matrix allocates 4 bytes per pair while triples use 12 bytes per occurring pair.", qHint: "Compare the layouts." },
  39: { concept: "triangular matrix details", why: "gives the formula for total pairs n(n-1)/2 and notes triples beat the matrix if fewer than 1/3 of pairs occur", pitfall: "forgetting the 1/3 threshold for when triples win", qType: "derive", qPrompt: "When does the triples approach beat the triangular matrix?", qAnswer: "When fewer than 1/3 of possible pairs actually occur, since triples use 12 bytes per occurring pair vs 4 bytes per pair for the matrix.", qHint: "Compare 12 vs 4 bytes." },
  40: { concept: "worked example: matrix vs triples", why: "shows a concrete calculation with 1e5 items and 1e7 baskets of 10 items each", pitfall: "miscounting the number of pairs or non-zero counts", qType: "apply", qPrompt: "In the example, how many integer counts does the triangular matrix require?", qAnswer: "About 5e9 (n(n-1)/2 for n=1e5), while triples need about 4.5e8 non-zero counts at worst.", qHint: "n(n-1)/2." },
  41: { concept: "lack of memory to fit all pairs", why: "raises the question of what to do when pairs do not fit in main memory, motivating the A-Priori algorithm", pitfall: "assuming all pairs can always be counted in one pass", qType: "explain", qPrompt: "What happens when the pairs do not fit in main memory?", qAnswer: "A single-pass counting approach fails, motivating algorithms like A-Priori that prune candidates to fit in memory.", qHint: "Prune to fit." },
  42: { concept: "A-Priori algorithm overview", why: "introduces the key ideas: monotonicity of frequent, candidate pairs, and extension to larger itemsets", pitfall: "treating A-Priori as a single-pass algorithm", qType: "define", qPrompt: "What are the key ideas behind the A-Priori algorithm?", qAnswer: "Monotonicity of frequent itemsets, the notion of candidate pairs, and extension to larger itemsets.", qHint: "Monotonicity + candidates." },
  43: { concept: "A-Priori: monotonicity and contrapositive", why: "states that if a set is frequent, every subset is frequent, and the contrapositive prunes pairs with infrequent items", pitfall: "forgetting the contrapositive direction for pruning", qType: "explain", qPrompt: "How does monotonicity help prune candidate pairs in A-Priori?", qAnswer: "If an item is not frequent, no pair containing it can be frequent, so such pairs are pruned.", qHint: "Contrapositive pruning." },
  44: { concept: "A-Priori two-pass approach", why: "describes pass 1 (count individual items) and pass 2 (count only pairs of frequent items)", pitfall: "forgetting that pass 2 only counts pairs of frequent items", qType: "explain", qPrompt: "What does each pass of A-Priori do?", qAnswer: "Pass 1 counts individual items to find frequent items; pass 2 counts only pairs where both elements are frequent.", qHint: "Items then pairs." },
  45: { concept: "A-Priori memory picture", why: "shows how main memory is used in pass 1 (item counts) and pass 2 (counts of candidate pairs)", pitfall: "ignoring the memory layout when analyzing the algorithm", qType: "orient", qPrompt: "How is main memory divided between the two passes of A-Priori?", qAnswer: "Pass 1 stores item counts; pass 2 stores counts of candidate pairs of frequent items.", qHint: "Two different memory uses." },
  46: { concept: "A-Priori detail: renumbering frequent items", why: "shows how to use the triangular matrix with n = number of frequent items by renumbering them", pitfall: "forgetting to keep a mapping between new and original item numbers", qType: "explain", qPrompt: "Why does A-Priori renumber frequent items?", qAnswer: "To use the triangular matrix with n = number of frequent items, which may save space compared to storing triples.", qHint: "Compact indexing." },
  47: { concept: "frequent triples and the C_k/L_k framework", why: "introduces candidate k-tuples C_k and frequent k-tuples L_k, with a filter/construct loop", pitfall: "confusing candidates with truly frequent itemsets", qType: "define", qPrompt: "What is the difference between C_k and L_k?", qAnswer: "C_k is the set of candidate k-tuples that might be frequent; L_k is the set of truly frequent k-tuples after counting.", qHint: "Candidates vs confirmed." },
  48: { concept: "candidate itemset generation details", why: "describes the two steps: union itemsets with k-2 common items, then prune those with an infrequent subset", pitfall: "forgetting the pruning step that removes candidates with infrequent subsets", qType: "explain", qPrompt: "What are the two steps of candidate itemset generation?", qAnswer: "Union pairs of itemsets with k-2 common items to form size-k candidates, then prune any whose size-(k-1) subsets are not all frequent.", qHint: "Generate then prune." },
  49: { concept: "worked example: forming candidate triplets", why: "shows the generation and pruning of candidate triplets from frequent pairs", pitfall: "forgetting to check all subsets when pruning", qType: "apply", qPrompt: "In the example, why is {1,2,3} ruled out as a candidate?", qAnswer: "Because {1,3} is not a frequent pair, so {1,2,3} cannot be frequent by monotonicity.", qHint: "Check all subsets." },
  50: { concept: "A-Priori for all frequent itemsets", why: "notes one pass per itemset size and that k=2 requires the most memory for typical data", pitfall: "assuming larger itemsets are the memory bottleneck", qType: "explain", qPrompt: "Why does k=2 require the most memory in A-Priori?", qAnswer: "Because the number of candidate pairs is typically much larger than the number of candidate triples or larger itemsets.", qHint: "Pairs dominate." },
  51: { concept: "section divider into hash functions", why: "marks the transition to hashing techniques used by PCY and related algorithms", pitfall: "skipping the divider and losing the hashing context", qType: "orient", qPrompt: "Why are hash functions introduced at this point?", qAnswer: "They are the key tool for the PCY algorithm and its refinements, which use hashing to prune candidate pairs.", qHint: "Hashing for pruning." },
  52: { concept: "hash function definition", why: "defines a hash function as mapping a hash-key to a bucket number, ideally uniformly", pitfall: "assuming any hash function distributes uniformly without checking", qType: "define", qPrompt: "What is a hash function and what property should it ideally have?", qAnswer: "It maps a hash-key to a bucket number in 0..B-1, ideally randomizing keys roughly uniformly into buckets.", qHint: "Uniform distribution." },
  53: { concept: "examples of hash functions", why: "shows hashing phone numbers by summing digits modulo B and hashing strings via ASCII codes", pitfall: "using a hash function that does not distribute well for the given key type", qType: "apply", qPrompt: "How would you hash a phone number to a bucket?", qAnswer: "Sum the digits modulo B, or partition into blocks and concatenate before summing modulo B for better distribution.", qHint: "Modulo arithmetic." },
  54: { concept: "indexing using hash functions", why: "shows how hashing enables quick search and retrieval by bucketing records", pitfall: "treating hashing as only for counting when it also enables indexing", qType: "apply", qPrompt: "How does hashing enable quick search and retrieval?", qAnswer: "By hashing a key (e.g., first letter of name) to a bucket index, so only records in that bucket need examination.", qHint: "Bucket = index." },
  55: { concept: "section divider into PCY algorithm", why: "marks the transition to the PCY (Park-Chen-Yu) algorithm", pitfall: "skipping the divider and losing the algorithmic context", qType: "orient", qPrompt: "What algorithm does this section introduce?", qAnswer: "The PCY (Park-Chen-Yu) algorithm, which uses hashing to reduce memory in pass 2 of A-Priori.", qHint: "Hashing to prune." },
  56: { concept: "PCY memory picture", why: "shows how PCY uses spare memory in pass 1 for a hash table, unlike A-Priori", pitfall: "forgetting that PCY exploits idle memory in pass 1", qType: "compare", qPrompt: "How does PCY's memory use differ from A-Priori's?", qAnswer: "PCY uses the spare memory in pass 1 to maintain a hash table of pair counts, which A-Priori leaves idle.", qHint: "Spare memory = hash table." },
  57: { concept: "PCY observation and approach", why: "observes that pass 1 of A-Priori leaves most memory idle and uses it to hash pairs into buckets", pitfall: "storing the actual pairs in buckets instead of just counts", qType: "explain", qPrompt: "What is the key observation behind PCY?", qAnswer: "Pass 1 of A-Priori leaves most memory idle, so PCY uses it to maintain a hash table of pair counts to prune candidates.", qHint: "Use idle memory." },
  58: { concept: "PCY first pass", why: "describes the first pass: count items and hash each pair to a bucket, incrementing bucket counts", pitfall: "forgetting that only bucket counts are kept, not the pairs", qType: "explain", qPrompt: "What does PCY do in its first pass?", qAnswer: "It counts individual items and hashes each pair to a bucket, incrementing the bucket count without storing the pairs.", qHint: "Count buckets, not pairs." },
  59: { concept: "using hash buckets to prune", why: "shows that a bucket with count below s cannot contain a frequent pair, so those pairs are pruned", pitfall: "assuming a frequent bucket contains a frequent pair", qType: "explain", qPrompt: "How does a low-count bucket help prune candidate pairs?", qAnswer: "If a bucket's total count is below s, none of its pairs can be frequent, so they are eliminated as candidates.", qHint: "Low bucket = prune." },
  60: { concept: "PCY example", why: "shows a concrete example of hashing pairs into buckets and the candidate conditions", pitfall: "forgetting that a pair must hash to a frequent bucket AND have frequent singletons", qType: "apply", qPrompt: "What two conditions must a pair satisfy to be a candidate in PCY?", qAnswer: "Both singletons must be frequent AND the pair must hash to a frequent bucket.", qHint: "Two conditions." },
  61: { concept: "PCY analysis", why: "shows why many buckets are expected to be infrequent, making PCY effective", pitfall: "forgetting the arithmetic that shows most buckets are infrequent", qType: "derive", qPrompt: "Why are most buckets expected to be infrequent in PCY?", qAnswer: "With 2.5e8 buckets and 4.5e10 total pairs, the average bucket count is 180, far below a typical support threshold.", qHint: "Average bucket count." },
  62: { concept: "PCY between passes: bit-vector", why: "replaces bucket counts with a bit-vector (1 = frequent bucket, 0 = not), using 1/32 of the memory", pitfall: "forgetting that the bit-vector compresses the hash table", qType: "explain", qPrompt: "How does PCY compress the hash table between passes?", qAnswer: "It replaces 4-byte integer counts with a bit-vector where 1 means the bucket exceeded support, using 1/32 of the memory.", qHint: "Bits not integers." },
  63: { concept: "PCY pass 2", why: "counts only pairs that are candidates: both items frequent AND pair hashes to a frequent bucket", pitfall: "forgetting both candidate conditions", qType: "explain", qPrompt: "What conditions must a pair meet to be counted in PCY pass 2?", qAnswer: "Both items must be frequent and the pair must hash to a bucket whose bit is 1 (frequent bucket).", qHint: "Frequent items + frequent bucket." },
  64: { concept: "PCY memory picture", why: "shows the memory layout: item counts and hash table in pass 1, bitmap and candidate pair counts in pass 2", pitfall: "ignoring the memory layout when analyzing PCY", qType: "orient", qPrompt: "How is memory divided in PCY's two passes?", qAnswer: "Pass 1 stores item counts and a hash table; pass 2 stores a bitmap and counts of candidate pairs.", qHint: "Two memory layouts." },
  65: { concept: "PCY main-memory details", why: "notes we need not count a bucket past s, and that triples are essential in pass 2, so PCY must eliminate ~2/3 of candidates to beat A-Priori", pitfall: "forgetting the 2/3 elimination threshold", qType: "explain", qPrompt: "Why must PCY eliminate about 2/3 of candidate pairs to beat A-Priori?", qAnswer: "Because pass 2 uses triples (12 bytes per pair) instead of the triangular matrix, so enough candidates must be pruned to save memory.", qHint: "Triples cost more." },
  66: { concept: "multistage algorithm refinement", why: "adds a third pass to further prune candidate pairs by rehashing only qualifying pairs", pitfall: "forgetting that multistage uses an additional pass", qType: "explain", qPrompt: "What is the key idea of the multistage algorithm?", qAnswer: "After PCY pass 1, rehash only pairs that qualify for pass 2, using a second hash function to further prune candidates.", qHint: "Rehash qualifying pairs." },
  67: { concept: "multistage memory picture", why: "shows three passes with two bitmaps and two hash tables", pitfall: "confusing the multistage and multihash memory layouts", qType: "orient", qPrompt: "How does multistage use memory across its three passes?", qAnswer: "Pass 1 hashes pairs; pass 2 rehashes qualifying pairs with a second hash function; pass 3 counts candidates meeting both bitmap conditions.", qHint: "Two hash tables." },
  68: { concept: "multistage pass 2", why: "uses a new independent hash function to hash only pairs that are frequent and hashed to a frequent bucket in pass 1", pitfall: "forgetting the hash functions must be independent", qType: "explain", qPrompt: "What pairs does multistage hash in pass 2?", qAnswer: "Only pairs where both items are frequent and the pair hashed to a frequent bucket with the first hash function.", qHint: "Qualifying pairs only." },
  69: { concept: "multistage pass 3", why: "counts only pairs meeting all three conditions: frequent items, frequent bucket in bitmap 1, frequent bucket in bitmap 2", pitfall: "forgetting to check both bitmaps in pass 3", qType: "explain", qPrompt: "What three conditions must a pair meet in multistage pass 3?", qAnswer: "Both items frequent, pair hashes to a frequent bucket in bitmap 1, and pair hashes to a frequent bucket in bitmap 2.", qHint: "Three conditions." },
  70: { concept: "important points: independence and both hashes", why: "emphasizes the two hash functions must be independent and both must be checked in pass 3", pitfall: "using dependent hash functions or checking only one bitmap", qType: "explain", qPrompt: "Why must both hashes be checked in multistage pass 3?", qAnswer: "Otherwise we would count pairs that hashed first to an infrequent bucket but happened to hash second to a frequent bucket.", qHint: "Both conditions required." },
  71: { concept: "multihash refinement", why: "uses several independent hash tables on the first pass, risking doubled average counts but saving a pass", pitfall: "forgetting the risk that halving buckets doubles average counts", qType: "compare", qPrompt: "What is the trade-off in multihash vs multistage?", qAnswer: "Multihash uses several hash tables in one pass (2 passes total) but risks doubling average bucket counts; multistage uses 3 passes with more pruning.", qHint: "Passes vs bucket counts." },
  72: { concept: "multihash memory picture", why: "shows two hash tables and two bitmaps in two passes, with combined bitmap size equal to one PCY bitmap", pitfall: "confusing multihash and multistage memory layouts", qType: "orient", qPrompt: "How does multihash use memory across its two passes?", qAnswer: "Pass 1 uses two hash tables; pass 2 uses two bitmaps (combined size of one PCY bitmap) and candidate pair counts.", qHint: "Two hash tables, two bitmaps." },
  73: { concept: "multihash pass 2", why: "counts pairs that are frequent and hash to frequent buckets in both hash functions", pitfall: "forgetting both hash conditions", qType: "explain", qPrompt: "What conditions must a pair meet in multihash pass 2?", qAnswer: "Both items frequent and the pair hashes to a frequent bucket for both hash functions.", qHint: "Both hashes frequent." },
  74: { concept: "multihash example", why: "shows the probability analysis: multihash reduces the chance of a random infrequent pair being a candidate", pitfall: "miscounting the probability calculations", qType: "derive", qPrompt: "Why does multihash reduce the chance of false candidates vs PCY?", qAnswer: "With two half-sized tables, a random infrequent pair has at most (1/5)^2 = 0.04 chance of hashing to frequent buckets in both, vs 0.1 for PCY.", qHint: "Square the fraction." },
  75: { concept: "adding more hash functions", why: "notes both multistage and multihash can use more than two hash functions, with diminishing returns", pitfall: "assuming more hash functions always helps", qType: "explain", qPrompt: "What are the limits of adding more hash functions?", qAnswer: "Multistage runs out of memory for bitmaps; multihash causes most buckets to become frequent, reducing pruning effectiveness.", qHint: "Diminishing returns." },
  76: { concept: "section divider into frequent itemsets in < 2 passes", why: "marks the transition to algorithms that find frequent itemsets in fewer than k passes", pitfall: "skipping the divider and losing the pass-reduction context", qType: "orient", qPrompt: "What does this section focus on?", qAnswer: "Algorithms that find frequent itemsets in fewer than k passes, potentially missing some (false negatives).", qHint: "Fewer passes." },
  77: { concept: "frequent itemsets in < 2 passes", why: "notes A-Priori takes k passes and introduces random sampling, SON, and Toivonen's algorithm", pitfall: "forgetting that fewer passes may introduce false negatives", qType: "compare", qPrompt: "What is the trade-off of using fewer passes?", qAnswer: "Fewer passes may miss some frequent itemsets (false negatives) but reduce disk I/O and runtime.", qHint: "Speed vs completeness." },
  78: { concept: "random sampling pass 1", why: "takes a random sample, runs A-Priori in memory with a proportionally reduced support threshold", pitfall: "forgetting to reduce the support threshold proportionally to the sample size", qType: "explain", qPrompt: "How does random sampling reduce the support threshold?", qAnswer: "If the sample is 1/100 of the baskets, use s/100 as the support threshold in the sample.", qHint: "Proportional threshold." },
  79: { concept: "random sampling pass 2", why: "optionally verifies sample-frequent itemsets against the full data, but may miss itemsets frequent only in the whole", pitfall: "forgetting that random sampling can produce false negatives", qType: "explain", qPrompt: "What is the caveat of random sampling?", qAnswer: "It may miss itemsets that are frequent in the whole dataset but not in the sample (false negatives).", qHint: "Sample may miss." },
  80: { concept: "SON algorithm pass 1", why: "processes the entire file in memory-sized chunks, running A-Priori on each, with no false negatives by the pigeonhole principle", pitfall: "confusing SON with random sampling", qType: "explain", qPrompt: "How does SON avoid false negatives?", qAnswer: "By the pigeonhole principle, an itemset frequent in the whole must be frequent in some chunk, so no false negatives occur.", qHint: "Pigeonhole principle." },
  81: { concept: "SON algorithm pass 2", why: "counts all candidate itemsets against the full dataset to remove false positives", pitfall: "forgetting that SON requires a full second pass", qType: "explain", qPrompt: "What does SON pass 2 do and what is its cost?", qAnswer: "It counts all candidate itemsets against the full dataset to remove false positives, requiring a full pass over the file.", qHint: "Verify candidates." },
  82: { concept: "Toivonen's algorithm pass 1", why: "finds proportionally frequent itemsets on a sample with a more conservative threshold, and records the negative border", pitfall: "forgetting the negative border concept", qType: "define", qPrompt: "What is the negative border in Toivonen's algorithm?", qAnswer: "An itemset that is not frequent in the sample but all its immediate subsets are frequent.", qHint: "Border of infrequency." },
  83: { concept: "negative border example", why: "shows a concrete example of when a 4-itemset is in the negative border", pitfall: "forgetting that all immediate subsets must be frequent", qType: "apply", qPrompt: "When is {A,B,C,D} in the negative border?", qAnswer: "When it is not frequent in the sample but all its 3-item subsets {A,B,C}, {B,C,D}, {A,C,D}, {A,B,D} are frequent.", qHint: "All subsets frequent." },
  84: { concept: "Toivonen's algorithm pass 2", why: "counts candidates and negative border itemsets, with two possible outcomes determining success or retry", pitfall: "forgetting the two-outcome logic", qType: "explain", qPrompt: "What are the two possible outcomes of Toivonen's pass 2?", qAnswer: "If no negative-border itemset is frequent, all candidates are verified; if one is frequent, the algorithm must repeat with a new sample.", qHint: "Success or retry." },
  85: { concept: "outcome 2: negative border frequent", why: "illustrates the case where a negative-border itemset is frequent, requiring a retry", pitfall: "treating outcome 2 as a failure rather than a retry signal", qType: "explain", qPrompt: "What does it mean if a negative-border itemset is frequent in the whole?", qAnswer: "The algorithm may have missed larger frequent itemsets, so it must repeat with a new random sample.", qHint: "Retry needed." },
  86: { concept: "theorem: no false negatives", why: "proves by contradiction that if a false negative exists, the negative border contains a frequent itemset, so the algorithm does not terminate", pitfall: "skipping the proof and losing the guarantee", qType: "derive", qPrompt: "Why does Toivonen's algorithm have no false negatives?", qAnswer: "By contradiction: if a frequent itemset is missed, a smallest infrequent subset is in the negative border and is frequent in the whole, so the algorithm retries.", qHint: "Contradiction proof." },
  87: { concept: "Toivonen's algorithm summary", why: "summarizes pros (no false positives/negatives, similar runtime to random sampling) and cons (may fail and require retry)", pitfall: "forgetting the retry risk", qType: "compare", qPrompt: "What are the pros and cons of Toivonen's algorithm?", qAnswer: "Pros: no false positives or negatives, similar runtime to random sampling. Cons: may fail if the threshold is not conservative enough, requiring a retry.", qHint: "Guarantees vs retry risk." },
  88: { concept: "comparison of frequent itemset algorithms", why: "compares Apriori, PCY, random sampling, SON, and Toivonen's on passes, false positives/negatives, memory, scalability, and candidate generation", pitfall: "forgetting which algorithm has which trade-off", qType: "compare", qPrompt: "Which algorithms have no false negatives and which do?", qAnswer: "Apriori, PCY, SON, and Toivonen's have no false negatives; random sampling can have false negatives.", qHint: "Check the table." },
  89: { concept: "lecture summary", why: "recaps Bonferroni's Principle, association rules (support, confidence, interestingness), and frequent itemset algorithms", pitfall: "treating the summary as a substitute for the derivations", qType: "recall", qPrompt: "What are the three main takeaways of this lecture?", qAnswer: "Bonferroni's Principle, association rules with support/confidence/interestingness, and frequent itemset algorithms measured by passes with monotonicity-based pruning.", qHint: "Three pillars." },
  90: { concept: "acknowledgement", why: "credits the original slide authors from mmds.org", pitfall: "treating the acknowledgement as technical content", qType: "orient", qPrompt: "Why is the acknowledgement included?", qAnswer: "It credits Prof. Jeffrey D. Ullman, Dr. Anand Rajaraman, and Dr. Jure Leskovec for allowing adaptation of their slides from mmds.org.", qHint: "Attribution." }
};

function buildExplanation(meta, title, text) {
  const t = (text || '').replace(/\s+/g, ' ').trim();
  return {
    whatYouSee: `The slide titled "${title}" presents: ${t.slice(0, 200)}${t.length > 200 ? '...' : ''}`,
    whyItMatters: meta.why,
    intuition: `This slide focuses on ${meta.concept}. Read it as part of the lecture's narrative arc rather than in isolation.`,
    technicalDetail: `The slide is part of the DSA5101 Lecture 1 source (${'DSA5101/Lec1 - Assoc Rules, Frequent itemsets.pdf'}), page ${'see sourceRef'}. The extracted text is a reader layer; the rendered slide remains authoritative.`,
    pitfall: `Do not fall into the trap of ${meta.pitfall}.`,
    connection: `This slide connects to the surrounding slides in the ${'frequent-itemset'} narrative.`
  };
}

function buildSocratic(meta) {
  return [
    { type: meta.qType, prompt: meta.qPrompt, answer: meta.qAnswer, hint: meta.qHint }
  ];
}

function main() {
  const data = JSON.parse(fs.readFileSync(FILE, 'utf8'));
  let updated = 0;
  for (const slide of data.slides) {
    const meta = META[slide.slideNumber];
    if (!meta) {
      console.warn(`No study metadata for slide ${slide.slideNumber}; skipping.`);
      continue;
    }
    const text = slide.extraction && slide.extraction.text ? slide.extraction.text : '';
    slide.explanation = buildExplanation(meta, slide.title, text);
    slide.socraticQuestions = buildSocratic(meta);
    updated++;
  }
  fs.writeFileSync(FILE, JSON.stringify(data, null, 2) + '\n', 'utf8');
  console.log(`Updated ${updated} slides with study layers.`);
}

main();
