"use strict";

// The Vercel project hosts only the server-side API. GitHub Pages owns the
// static Atlas build, so do not run the heavyweight Pages prerender here.
console.log("NUS Atlas API build: server functions only");
